# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Repo-specific rules live in `.claude/rules/`:

- `repo-overview.md` — what this repo is and its constraints (no build system).
- `running-and-developing.md` — how to preview/run the prototype.
- `index-html-architecture.md` — internal structure of `PMS index.html` (loaded when that file is in play).
- `docs-relationship.md` — how `PMS index.html`, `REQUIREMENTS_PHASE1.md`, and `PMS sitemap.txt` relate, and how to cross-reference them before implementing a feature.

@AGENTS.md
